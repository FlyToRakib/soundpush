[package]
name = "sp-testkit"
description = "SoundPush test kit: simulated network links, generated audio and measurement helpers"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
sp-engine = { path = "../sp-engine" }
sp-media = { path = "../sp-media" }
sp-protocol = { path = "../sp-protocol" }
bytes = "1"
rand = "0.8"

[lints]
workspace = true
